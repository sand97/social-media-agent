import type { ThemeConfig } from 'antd'

export const theme: ThemeConfig = {
  token: {
    // Seed Token
    colorPrimary: '#00b96b',
    borderRadius: 2,

    colorLink: '#00b96b',
    // Alias Token
    colorBgContainer: '#ffffff',
  },
}
